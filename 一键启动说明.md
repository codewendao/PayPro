# PayPro 一键启动说明

## 环境要求

- Linux 服务器（Ubuntu 18.04+ / CentOS 7+ / Debian 10+）
- CPU 1 核 / 内存 2 GB / 磁盘 20 GB
- 已安装 Docker（命令行输入 `docker` 验证）

> 小白建议选购服务器时直接选带 Docker 的镜像。

---

## 启动步骤

### 1. 配置 Docker 镜像加速（国内服务器必做）

国内服务器无法直连 Docker Hub，需配置镜像源：

```bash
sudo systemctl restart docker
sudo tee /etc/docker/daemon.json << 'EOF'
{
  "registry-mirrors": ["https://mirror.ccs.tencentyun.com"]
}
EOF
sudo systemctl restart docker
```

### 2. 一键启动

```bash
cd paypro
bash install.sh
```

`install.sh` 会自动安装项目自带的 Docker Compose 并启动。如果安装失败，手动执行：

```bash
DOCKER_CONFIG=${DOCKER_CONFIG:-$HOME/.docker}
mkdir -p $DOCKER_CONFIG/cli-plugins
cp docker-compose $DOCKER_CONFIG/cli-plugins/
chmod +x $DOCKER_CONFIG/cli-plugins/docker-compose
bash install.sh
```

> **Docker 权限**：若报权限错误，将当前用户加入 docker 组：
> ```bash
> sudo usermod -aG docker $USER
> newgrp docker
> ```

### 3. 访问

浏览器打开 `http://<服务器IP>:8889`

> 先尝试能不能访问，不能访问再开启，因为现在的服务器默认是开启防火墙的。
>
> 防火墙需放行 `8889` 端口：
> ```bash
> firewall-cmd --add-port=8889/tcp --permanent && firewall-cmd --reload   # CentOS
> ufw allow 8889/tcp                                                       # Ubuntu
> ```
> 如果仍然不能访问请检查项目日志是否有报错，如果没有报错请检查服务器安全组是否开启8889端口，如何查看取决于各服务器运营商。

---

## 日常维护

> 以下命令均在项目目录下执行：`cd pay`

| 操作 | 命令 |
|------|------|
| 启动 | `docker compose up -d` |
| 停止 | `docker compose down` |
| 重启（全部） | `docker compose restart` |
| 重启（仅应用） | `docker compose restart pay` |
| 查看状态 | `docker compose ps` |
| 查看日志（实时） | `docker compose logs -f pay` |
| 查看日志（最近100行） | `docker compose logs --tail 100 pay` |

### 修改配置

`config/application-prod.yml` 已提供模板，修改后重启生效：

```bash
docker compose restart pay
```

### 更新源码

```bash
git pull
docker compose up -d --build
```

### 重置数据

```bash
docker compose down -v    # 删除容器 + 数据卷
bash install.sh           # 重新部署
```

---

## 配置自己的收款二维码

部署成功后，用你自己的微信收款码替换系统默认二维码。

> 前置条件：`config/application-prod.yml` 中已配置 `qr-dir: /app/appsystems/qr`（模板默认已配好）。

**第一步：了解你要配哪些二维码**

打开 `config/application-prod.yml`，找到 `payMethods`，看哪些支付方式开启了 `use-local-qr-code: true`：

- `wechat` 微信收款码
- `wechat_zs` 微信赞赏码
- `alipay`  支付宝收款码

**第二步：创建目录结构**

以微信支付、两个固定金额（5.00 和 10.00）、二维码数量为 2 为例：

```
qr/
└── wechat/
    ├── 5.00/
    │   ├── 1.png      # 5.00 的二维码变体1
    │   └── 2.png      # 5.00 的二维码变体2
    ├── 10.00/
    │   ├── 1.png      # 10.00 的二维码变体1
    │   └── 2.png      # 10.00 的二维码变体2
    └── custom.png     # 用户自定义金额时的兜底二维码
```

**路径规则：**

| 场景 | 路径格式 | 示例 |
|------|---------|------|
| 固定金额 | `{支付类型}/{金额}/{编号}.png` | `wechat/5.00/1.png` |
| 自定义金额兜底 | `{支付类型}/custom.png` | `wechat/custom.png` |

> `{编号}` 从 1 开始，数量对应配置文件中的 `qrCodeNum`。多编号用于区分并发订单。
>
> **覆盖与回退**：外部有的覆盖内置，外部没有的自动用内置。只想新增 6.00 的码？只放 `qr/wechat/6.00/` 即可，JAR 里的 5.00 照常工作。

**第三步：放入二维码并验证**

把收款二维码截图/导出为 PNG，按上述目录放好后：

```bash
docker compose restart pay
```

打开 `http://你的IP:8889`，选择对应金额发起支付，确认页面显示的是你放入的二维码。

---

## 常见问题

**Q: 报错 "request canceled while waiting for connection" 或拉取镜像超时？**
国内服务器需配置 Docker 镜像加速，参见上方「配置 Docker 镜像加速」。

**Q: 启动后无法访问？**
- `docker compose ps` 确认所有服务状态为 healthy
- 检查防火墙放行 `8889` 端口
- `docker compose logs pay` 查看日志

**Q: 修改源码后如何更新？**
`docker compose up -d --build` 重新构建

**Q: 如何重置所有数据？**
`docker compose down -v` 清空所有数据卷，再 `bash install.sh` 重新部署
